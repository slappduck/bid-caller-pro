# Serve the landing page at the site root.
#
# Cloudflare's asset config here uses html_handling = "none", so paths are
# served exactly as written -- /app.html comes from /app.html with no redirect
# hop. That is deliberate: sw.js precaches explicit ".html" paths, and
# precaching a URL that 307s to somewhere else is fragile.
#
# The cost of literal paths is that "/" no longer maps to index.html on its
# own, which returned a bare 404 on the workers.dev deploy. This restores it
# as a 200 rewrite rather than a redirect, so the address bar stays on "/"
# and nothing gains an extra round trip.
#
# Same file format on Netlify and Cloudflare, so this is correct on whichever
# host is serving.
/    /index.html    200

# Extensionless aliases.
#
# html_handling = "none" means /app is a plain 404 -- only /app.html exists.
# That bit already: the app was reached at curbcallpro.com/app from a phone
# and only rendered because the service worker had app.html cached. A first
# time visitor typing the same address gets nothing.
#
# These are 200 rewrites, not redirects, for the same reason "/" is: the URL
# stays as typed and no hop is added, so sw.js's precache of the explicit
# ".html" paths is untouched.
/app            /app.html          200
/post-a-bid     /post-a-bid.html   200
/privacy        /privacy.html      200
/terms          /terms.html        200

# Per-recipient outreach links.
#
# 200 rewrites, not redirects, and that is the whole trick: the address bar
# keeps saying /go/marshall while the landing page is served from
# index.html. The analytics beacon reports whatever the address bar says, so
# each contractor shows up as its own row without a second page existing.
#
# A redirect would defeat this -- the browser would land on "/" and every
# click would collapse into one indistinguishable number.
#
# These are cold-outreach links, so they are deliberately readable rather
# than hashed. Anyone who finds one just sees the normal landing page.
/go/marshall      /index.html   200
/go/mattingly     /index.html   200
/go/1c39          /index.html   200
/go/concreteindy  /index.html   200
/go/brbs          /index.html   200
/go/acr           /index.html   200
/go/pcap          /index.html   200
/go/clauss        /index.html   200
/go/morici        /index.html   200
/go/plm           /index.html   200
/go/advconc       /index.html   200
/go/csi           /index.html   200
/go/moate         /index.html   200
/go/dmconc        /index.html   200
/go/stalls        /index.html   200
/go/procon        /index.html   200
/go/affasph       /index.html   200
/go/encore        /index.html   200
/go/springfield-mo-eutsler       /index.html   200
/go/columbia-mo-ccg              /index.html   200
/go/columbia-mo-como             /index.html   200
/go/columbia-mo-concrete         /index.html   200
/go/columbia-mo-concreteco       /index.html   200
/go/jeffcity-mo-midmo            /index.html   200
/go/jeffcity-mo-allseasons       /index.html   200
/go/springfield-il-cc            /index.html   200
/go/springfield-il-superior      /index.html   200
/go/springfield-il-experts       /index.html   200
/go/springfield-il-meyers        /index.html   200
/go/peoria-il-services           /index.html   200
/go/peoria-il-gg                 /index.html   200
/go/bloomington-il-baseline      /index.html   200
/go/nashville-tn-gentlemen       /index.html   200
/go/nashville-tn-jones           /index.html   200
/go/nashville-tn-nashvilleconcrete /index.html 200
/go/nashville-tn-turner          /index.html   200
/go/knoxville-tn-cresco          /index.html   200
/go/knoxville-tn-mtz             /index.html   200
/go/knoxville-tn-kings           /index.html   200
/go/knoxville-tn-united          /index.html   200
/go/chattanooga-tn-ccc           /index.html   200
/go/murfreesboro-tn-elite        /index.html   200
/go/murfreesboro-tn-cantrells    /index.html   200
/go/murfreesboro-tn-concreteco   /index.html   200
